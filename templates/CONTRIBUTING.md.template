<!-- BEGIN MEAVO RELEASE POLICY -->
## Branches and production permission

Create `feat/`, `fix/`, or `chore/` branches from `staging` and open PRs explicitly into `staging`; squash after required checks pass. Never push directly to `main` or `staging`. Production releases use a `staging` → `main` PR and a merge commit. AI agents require explicit human authorization for the repository, production action, and current reviewed PR/head SHA or artifact/configuration scope before merging, enabling auto-merge, queueing, or making any production change. See [RELEASE_POLICY.md](RELEASE_POLICY.md) for the mandatory approval and environment checks; missing staging does not authorize a main release.
<!-- END MEAVO RELEASE POLICY -->

# Contributing — <!-- FILL: repo name -->

## Before you open a PR

- [ ] Changes are scoped to the request — no drive-by refactors
- [ ] `<!-- FILL: lint command -->` passes
- [ ] `<!-- FILL: test command or "No test suite — manual check documented" -->`
- [ ] Agent docs updated if you added routes, domain modules, crons, or auth rules
- [ ] <!-- FILL: e.g. enqueueSheetSync called after writes that must reach external systems -->

## Branch naming

Use `feat/short-description`, `fix/issue-123`, or `chore/agent-docs` from `staging`. Explicitly set the PR base to `staging`.

## Commit messages

<!-- FILL: e.g. imperative mood, complete sentences: "Add factory-fill cron guard" -->

## Code placement

| Layer | Location |
|-------|----------|
| <!-- FILL: UI/pages --> | <!-- FILL: path --> |
| <!-- FILL: API / actions --> | <!-- FILL: path --> |
| <!-- FILL: business logic --> | <!-- FILL: path --> |
| <!-- FILL: integrations --> | <!-- FILL: path --> |

## Cross-repo dependencies

<!-- FILL: how to bump @meavo/db, @meavo/navigation, etc. Or "N/A". -->

## Schema changes

<!-- FILL: repo-specific schema ownership and validation, or N/A. Production migrations and consumed release-tag publication require human permission under RELEASE_POLICY.md. -->

## PR description

Include:

1. **What** changed (user-visible or API behaviour)
2. **Why** (link issue if any)
3. **How to verify** (commands or manual steps)
4. **Out of scope** (what you intentionally did not change)

## Agent-assisted PRs

If an AI agent wrote the code:

- Verify paths and business rules against `docs/domain.md`
- Reject placeholder text (`<!-- FILL:`) in merged files
- Ensure no secrets in diff
